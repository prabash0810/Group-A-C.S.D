# Images

Place project image assets in this folder.

The current DayWise interface uses CSS gradients, emoji and inline SVG data, so no standalone image files were moved during the reorganisation.

When adding an image, reference it from an HTML page with `../Images/filename.ext` or from a stylesheet with `../Images/filename.ext`.
